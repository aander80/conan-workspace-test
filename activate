SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

source $SCRIPT_DIR/.venv/bin/activate

# TODO: Can this be configured in conanws.yml?
export CONAN_HOME=$SCRIPT_DIR/.conan2
